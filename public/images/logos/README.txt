Drop company logos here as PNG (transparent background ideal) to replace the text on the
"Everyone is scrambling for compute" slide. Exact filenames:
  nvidia.png  amd.png  google.png  aws.png  cerebras.png   (column headers)
  openai.png  xai.png  anthropic.png                        (the bubbles)
If a file is missing, the slide just shows the styled text name instead.
